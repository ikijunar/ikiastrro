---
last_updated: 2026-09-07
---

# UI implementation resume

Status: transit data/selector implementation verified; broader visual and movement work remains.

## Scope and decisions

User asked to resume UI implementation previously blocked by write access and to check STANDARDS.md.
Read D:\@ClaudeSpace\STANDARDS.md, project entry instructions, README, master index, history,
launcher notes and the transit specification. Existing Home implementation was preserved.
No change to the shared standards was needed.

Branch: `chore/github-pm-templates`; original checkout and unrelated dirty changes preserved.
Reference: `ui_specs_transit_wheel_v1.2.md`, including dated amendments and drag rollback.

## Implemented

- Load the selected saved person's latest D1 and its natal grahas, Gulika/Maandi and Ascendant.
- Show an explicit unavailable state for missing people, D1/Ascendant or invalid saved offsets; bare transit route returns to Home.
- Replace placeholder month offsets with saved Mahadasha/Antardasha periods.
- Keep a single active UTC instant, display IST, preserve exact period start time and interpret saved period wall times with the birth offset.
- Populate all nine transit comparison values from Swiss positions, deriving Ketu from Rahu + 180 degrees.
- Replace the template's static centre date with active date, Moon nakshatra/pada, Saturn/Jupiter motion/sign and selected dasha.
- Align zodiac labels with the template's Aries-zero nakshatra origin; retain supplied artwork.
- Keep the wheel square, preserve the comparison table on mobile and wrap navigation to eliminate measured mobile overflow.
- Use shared CSS tokens for the transit page palette.

Files: `TransitWheel.razor`, its isolated CSS, `TransitSelection.cs`,
`MainLayout.razor.css`, `tokens.css`, `TransitSelectionTests.cs`,
`scripts/verify-transit-ui.mjs` and the affected documentation.

## Verification

- Baseline and corrected Web build: zero warnings/errors.
- `dotnet test tests/Ikiastrro.Web.Tests/Ikiastrro.Web.Tests.csproj --no-restore -v quiet`: 38 passed, 0 failed.
- `node scripts/verify-transit-ui.mjs`: passed against a separate hidden Edge CDP session on 9229 and Development Web host on 5099.
- Browser checked date updates, Maha/Antar updates, preserved parent selection, unchanged natal SVG through time changes, distinct natal coordinates for saved people 1/2, square sizing, desktop fold, mobile comparison visibility, no horizontal overflow and no captured JavaScript exceptions.
- Desktop 1440×900: wheel 780×780, bottom 899.59. Mobile 390×844: wheel 327×327, no horizontal overflow.
- Screenshots: `reports/transit-wheel-desktop.jpg`, `reports/transit-wheel-mobile.jpg`; visually inspected.
- Runtime verification uses the existing Gochara snapshot persistence path; no migration or saved natal chart regeneration was performed.
- Initial runtime test exposed an unregistered terminology service; fixed by building the existing repository catalogue through the registered connection factory.
- Initial layout host omitted compiled CSS in Production; reran in Development. Mobile nav overflow was then reproduced and fixed.
- Whole-checkout diff check finds existing EOF blank lines in App.razor and Home.razor.css; these unrelated changes were preserved.

## Remaining work / next step

This is not full acceptance of every historical visual requirement. Outer fast-body labels can crowd near conjunctions; small SVG text is difficult to read on mobile and full period ranges are truncated in compact selectors. Next: improve label placement/legibility against the supplied reference, without changing true angular placement.
Dragging, exact multi-occurrence ephemeris solving and sampled movement tails remain unimplemented/disabled under the earlier rollback. Snapshot persistence still follows the existing repository's slow-body coverage.
No commit or push. Project edits and build execution required the runtime escalation route because the Windows sandbox helper intermittently fails with `helper_unknown_error: setup refresh had errors`.

