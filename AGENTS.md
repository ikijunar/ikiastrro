---
last_updated: 2026-09-06
---

# Shared agent instructions

Before work, read `D:\@ClaudeSpace\STANDARDS.md` and apply its relevant rules.
This is the canonical policy for both Claude Code and Codex. Do not infer that this pointer
automatically loaded the file. If access fails, report it before dependent changes.

Read the project's README.md, master_<name>.md index if present, and relevant active plan
or history before editing. Inspect actual Git status and preserve unrelated work.
Use the same project files for handoff (§F), concurrency (§R), and completion evidence (§S).
Do not use private tool memory as the only record of decisions or remaining work.

Keep paths under the verified workspace with the literal @. In WSL, verify the Windows
drive mount before using its equivalent path. In an external worktree, retain the canonical
standards pointer. Follow runtime permissions; this file does not grant filesystem access.
